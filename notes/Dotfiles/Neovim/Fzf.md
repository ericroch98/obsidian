#config #dotfiles #manuell
Läuft nicht mit neuester Version im APT-Repo. Daher zuerst die aktuellste Version über github installieren und bash neu laden.

```bash
git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
~/.fzf/install
```
